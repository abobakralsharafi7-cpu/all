import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.professor.proofdaftar",
  appName: "Proof Daftar",
  webDir: "out",
  android: {
    allowMixedContent: false,
  },
};

export default config;
