# تقرير تصحيح الحركات المستوردة إلى سندات قبض وصرف

## الهدف

تحويل الحركات المستوردة (69 حركة) إلى **سندات قبض وصرف أصلية** داخل البرنامج، بدل أن تكون قيودًا عامة، وفق القاعدة التالية:

| الحركة | نوع السند | بادئة الرقم | الصندوق | الحساب |
|---|---|---|---|---|
| موجبة | سند قبض | `coll` | **مدين** | **دائن** |
| سالبة | سند صرف | `Voc` | **دائن** | **مدين** |

## الملفات

- المدخل: `backup_1784979125243.db`
- **المخرج: `backup_1784979125243_2.db`**
- سكربت التصحيح: `اصلاح_السندات.py`

| الملف | SHA-256 |
|---|---|
| `backup_1784979125243.db` | `d15769ca10825d6834308f90f8db6a8dd42fe570e4ffb775de1c159196f7ef99` |
| `backup_1784979125243_2.db` | `dfc2dd6b6d387e691a9dd96a5c7f3407bc72468f6bf42098300faafb0d069562` |

---

## العطل الجوهري الذي تم إصلاحه

الحركات المستوردة سابقًا كانت مسجّلة بـ **`LineSeq = 0`**، بينما شاشة الحركات في البرنامج (`view_Transactions`) تعرض السندات عبر ربط `LineSeq=1` مع `LineSeq=2` فقط.

**النتيجة: كانت 69 حركة مستوردة مخفيّة تمامًا عن شاشة البرنامج.**

| | قبل التصحيح | بعد التصحيح |
|---|---:|---:|
| السندات الظاهرة في شاشة الحركات | **10** | **79** |

كما كانت الحركات تحمل أرقام مستندات بصيغة `legacy-acct-N` وتُسجَّل باتجاه معكوس (الحساب مدين عند الإيداع)، فأصبحت الآن سندات كاملة بأرقام البرنامج واتجاه محاسبي صحيح.

---

## ما تم تنفيذه

### 1. اتجاه القيد

أُعيدت كتابة طرفَي كل حركة:

- **سند قبض** (`TransID=3`): السطر الأول الصندوق **مدين**، السطر الثاني الحساب **دائن**.
- **سند صرف** (`TransID=2`): السطر الأول الصندوق **دائن**، السطر الثاني الحساب **مدين**.

### 2. أرقام السندات

صيغة الترقيم مستنبطة من السندات الأصلية داخل نفس القاعدة:

```
البادئة + السنة + رقم الشهر (بلا صفر بادئ) + التسلسل
مثال أصلي:  coll202663  =  coll + 2026 + 6 + 3
```

التسلسل يبدأ بعد آخر رقم مستخدم فعليًا في القاعدة، فلا يتعارض مع أي سند قائم:

| النوع | عدد السندات | نطاق التسلسل | أول رقم | الإجمالي |
|---|---:|---|---|---:|
| سند قبض `coll` | 44 | 10 – 53 | `coll2026610` | 3,596,950 |
| سند صرف `Voc` | 25 | 2 – 26 | `Voc202662` | 3,593,450 |

### 3. بنية السطور

كل سند أصبح سطرين بـ `LineSeq = 1` و `2`، مع `ItemID=0` و `Price=NULL` تمامًا كالسندات الأصلية، بدل `LineSeq=0` و `ItemID=1/2` السابقة.

### 4. حركة الرصيد الافتتاحي

الحركة رقم 1 «رصيد افتتاحي» بقيمة صفر سُجّلت **سند قبض فعلي** (`coll2026610`) بطرفين صفريين، فأصبحت ظاهرة وحركية في الشاشة مثل بقية السندات.

### 5. خانة المرجع

`RefernceID` أصبحت **فارغة** مطابقةً للسندات الأصلية، مع الإبقاء على `DOC_UNID` المخفي (`legacy-acount-details-N`) للتتبع الداخلي ومنع تكرار الاستيراد.

---

## اختبارات السلامة

| الاختبار | النتيجة |
|---|---|
| فحص سلامة SQLite | ناجح (`ok`) |
| فحص المفاتيح الأجنبية | ناجح (0 خطأ) |
| مطابقة كل حركة من ملف المصدر (المبلغ، التاريخ، البيان، الاتجاه) | ناجح لكل الـ69 |
| بادئة كل سند مطابقة لنوعه (`coll` قبض / `Voc` صرف) | ناجح |
| اتجاه المدين/الدائن للصندوق والحساب | ناجح |
| توازن كل سند (مدين = دائن) | ناجح لكل الـ79 |
| كل سند من سطرين `LineSeq` 1 و 2 | ناجح |
| عدم تعارض أرقام السندات مع القائمة | ناجح |
| ظهور كل السندات في شاشة الحركات | ناجح (79) |
| السجلات العشرون الأصلية دون تغيير | ناجح |
| مخطط قاعدة البيانات دون تغيير | ناجح |
| بقية الجداول دون تغيير | ناجح |

## الأرصدة

- عدد الحركات المستوردة: **69** (44 قبض + 25 صرف)
- عدد الأسطر: **138** سطرًا متوازنًا
- إجمالي المقبوضات: **3,596,950 ريال**
- إجمالي المصروفات: **3,593,450 ريال**
- **صافي رصيد الحساب: 3,500 ريال دائن** ← مطابق للمصدر
- **صافي أثر السندات على الصندوق: 3,500 ريال مدين**
- إجمالي أسطر `Transactions`: **158** (دون تغيير عن المدخل)
